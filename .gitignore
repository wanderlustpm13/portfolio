# Dependencies
node_modules/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Playwright / test artifacts (dev-only)
playwright-report/
test-results/
package.json
package-lock.json

# Original uncompressed project videos (only the optimized .mp4 derivatives are committed)
assets/projects/*.mov
assets/projects/*.MOV

# Original full-res strip photos (only the optimized strip/ + large/ derivatives are committed)
assets/images/*.jpg
assets/images/*.JPG
assets/images/*.jpeg
assets/images/*.JPEG
assets/images/*.png
assets/images/*.PNG
